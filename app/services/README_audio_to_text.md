# Audio answers are transcribed via Groq Whisper (`GROQ_WHISPER_MODEL`).
# Endpoint: POST /api/v1/interviews/sessions/{id}/audio-answer
# See app/services/ (transcription helpers) and frontend InterviewRoom mic capture.
