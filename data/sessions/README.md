# Interview session storage

Each completed or in-progress interview is saved here as one JSON file:

```
{session_id}.json
```

Files are created/updated automatically by the backend when you create a session, submit answers, or end an interview.

Restarting the API server will reload sessions from these files.
