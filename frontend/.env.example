# Backend API URL (no trailing slash)
# Cross-device LAN dev: leave unset — app uses relative /api and Vite proxies to :8080
# VITE_API_URL=
#
# Only set this if the browser must call the backend directly (e.g. separate ngrok tunnel):
# VITE_API_URL=http://192.168.1.6:8080
